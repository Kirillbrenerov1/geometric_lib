# Math formulas
## Area
- Circle: S = mR?
- Rectangle: 5 = ab
- Square: S = a2
## Perimeter
- Circle: P = 2nR
- Rectangle: P = 2(a + b)
- Square: P = 4a
# Functions
## Circle
### Area evaluation
*(r- radius)* 
Возвращает площадь круга.
    Параметры :
        r (int): радиус круга
    Возвращаемое значение:
        area(r)(int): площадь круга
```python 
def area(r):
```
›Example: area (1) returns 3.14159265359
### Perimeter evaluation
*(r- radius)* 
Возвращает периметр.
    Параметры :
        r (int): радиус фигуры
    Возвращаемое значение:
        perimeter(r)(int): периметр фигуры
```python
def perimeter(r):
```
›Example: perimeter (1) returns 6.28318530718
##Rectangle
### Area evaluation
*(a,b - sides)* 
Возвращает площадь фигуры.
    Параметры :
        a (int): сторона фигуры b (int): сторона фигуры
    Возвращаемое значение:
        area(a, b)(int): площадь фигуры
```python
def area(a, b):
```

›Example: area (1,2) returns 2
### Perimeter evaluation
*(a,b - sides)* 
Возвращает периметр фигуры.
    Параметры :
        a (int): сторона фигуры b (int): сторона фигуры
    Возвращаемое значение:
        perimeter(a, b)(int): периметр фигуры
```python
def perimeter(a, b)
```
›Example: perimeter (1, 2) returns 6
## Square
### Area evaluation
*(a - side)* 
Возвращает площадь фигуры.
    Параметры :
        a (int): сторона фигуры 
    Возвращаемое значение:
        area(a)(int): площадь фигуры
```python
def area (a):
```
### Perimeter evaluation
*(a,b - sides)* 
Возвращает периметр фигуры.
    Параметры :
        a (int): сторона фигуры 
    Возвращаемое значение:
        perimeter(a)(int): периметр фигуры
```python
def perimeter(a, b)
```
›Example: perimeter (1, 2) returns 6
## Square
### Area evaluation
*(a - side)* 
Возвращает площадь фигуры.
    Параметры :
        a (int): сторона фигуры 
    Возвращаемое значение:
        area(a)(int): площадь фигуры
```python 
def area(a):
```
›Example: area(2) returns 4
### Perimeter evaluation
*(a - side)* 
Возвращает периметр фигуры.
    Параметры :
        a (int): сторона фигуры 
    Возвращаемое значение:
        perimeter(a)(int): периметр фигуры
```python
def perimeter (a):
```
›Example: perimeter (1) returns 4
## Triangle
### Area evaluation
*(a - base, h - height)* 
Возвращает площадь фигуры.
    Параметры :
        a (int): сторона фигуры h (int): высота фигуры
    Возвращаемое значение:
        area(a,h)(int): площадь фигуры
```python
def area(a, h) :
```
›Example: area (1, 2) returns 1
### Perimeter evaluation
*(a,b,c - sides)* 
Возвращает площадь фигуры.
    Параметры :
        a (int): сторона фигуры b (int): сторона фигуры c (int): сторона фигуры
    Возвращаемое значение:
        periment(a,b,c)(int): периметр фигуры
```python
def perimeter(a, b, c) :
```
# Changelog
- [добавил комментарий](https://github.com/Kirillbrenerov1/geometric_lib/commit/203c4c6ef17661087ee87dcb3f8d75d6e3822195)
- [add triangle.py](https://github.com/Kirillbrenerov1/geometric_lib/commit/94efc71fca9bdc6866c687bcba29126181aba074)
-[add rectangle .py](https://github.com/Kirillbrenerov1/geometric_lib/commit/5e1a3b7ae9bf1e4c579b9f3ba15746f0491174a6)
-[L-03: Docs added](https://github.com/Kirillbrenerov1/geometric_lib/commit/d078c8d9ee6155f3cb0e577d28d337b791de28e2)
-[L-03: Circle and square added](https://github.com/Kirillbrenerov1/geometric_lib/commit/8ba9aeb3cea847b63a91ac378a2a6db758682460